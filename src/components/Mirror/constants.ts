/** LOD 切り替えのデフォルト距離（メートル） */
export const DEFAULT_LOD_DISTANCE = 10

/** ヒステリシス比率（Near→Far: lodDistance, Far→Near: lodDistance * ratio） */
export const LOD_HYSTERESIS_RATIO = 0.8

/** 反射テクスチャ更新間隔のデフォルト（2 = 2フレームに1回更新） */
export const DEFAULT_REFLECTION_INTERVAL = 2
